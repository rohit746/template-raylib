# template-raylib

Simple Template for ray lib library just download ray lib with mingGW and clone this repo
You should then see a popup to Load the Workspace to use it in VScode just click on that
and press `F5` to run the main.c fille
